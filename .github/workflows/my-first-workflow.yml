name: my-workflow

on: push

jobs:
  first:
    runs-on: ubuntu-latest

    steps:
      - name: Print message
        run: echo "Hello Rahul, how are you?"

       
      - name: show content of /etc/passwd
        run: sudo cat /etc/passwd